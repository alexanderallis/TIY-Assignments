var string = "";
countryName(names){
      var letters = names.split("");
      for (i=0; i< letters.length; i++){
          if(words[i] !== " "){
          var words = [];
          letters[i].push(words);
          }
          else(){

          }
      }
}
